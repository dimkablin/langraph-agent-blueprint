from __future__ import annotations


SKILL_SELECTION_PROMPT = """Use the named skill when it directly matches the task.
Skills narrow the assistant's focus; tools still obey permissions and allowed-tools rules."""

