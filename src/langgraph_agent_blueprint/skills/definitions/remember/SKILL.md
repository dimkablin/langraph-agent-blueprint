---
name: remember
description: Store durable user, project, or session memory.
allowed_tools:
  - read_file
  - write_file
---
Convert the user's memory request into a concise memory note and store it in the requested scope. Arguments: {{args}}

